class Question:
    def __init__(self, prompt, answer):
        self.prompt = prompt
        self.answer = answer
#這一段定義了一個 Question 類，有 prompt 和 answer 兩個屬性。
#在 __init__ 函數中，prompt 屬性儲存問題的提示，而 answer 屬性儲存問題的答案。